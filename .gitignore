node_modules
dist
out
recordings
.worktrees/
*.log
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example

.claude/settings.local.json

# Credentials file — never commit real keys (injected at build time only).
backend.env
backend/backend.env
backend/third_party/ffmpeg/

# Rust build output under native/ (e.g. audio-endpoint-monitor) — hundreds of
# MB of artifacts; sources (Cargo.toml, src/) remain committable.
native/**/target/
# Staged helper exe for electron-builder (scripts/stage-audio-helper.cjs)
native/audio-endpoint-monitor/dist/

# Spike scaffolding (12 Aug 2026 meeting-call-events spike): holds live secrets
# (.env already ignored above), a private key, and events.jsonl with real join
# URLs + participant names (PII). Evidence stays local only - never commit.
spikes/
