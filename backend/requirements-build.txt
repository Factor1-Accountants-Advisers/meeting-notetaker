# Build-time dependencies for the PyInstaller backend bundle.
# Keep separate from requirements.txt — these are only needed for
# packaging, not for the development server or CI backend tests.

pyinstaller>=6.0,<7.0
