# Backend skeleton deps. Azure SDKs, pyannote, and DB drivers land with the
# pipeline work (keep this list lean until then).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.0
# OS trust-store TLS verification (CryptoAPI on Windows); see app/tls.py.
truststore==0.10.4
