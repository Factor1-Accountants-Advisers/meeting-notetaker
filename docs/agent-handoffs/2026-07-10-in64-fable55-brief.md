# IN-64 Fable 5.5 / Claude Code implementation brief

Date: 2026-07-10
Repo: `/home/josephmiguelguerrero/projects/meeting-notetaker-2`
Windows repo, if needed: likely `C:\projects\meeting-notetaker-2` or Joseph's synced Windows checkout.
Branch at handoff: `main`, clean before this brief was written.

## Operating instructions for Fable 5.5 in Claude Code

You are working on the Factor1 Meeting Notetaker desktop app. Read these first:

1. `AGENTS.md` — canonical architecture, product rules, commands, and current Slice 1 scope.
2. `CLAUDE.md` — Claude Code machine-specific gotchas.
3. `docs/jira-progress.md` — evidence ledger; update only after verified changes.
4. `docs/rollout-runbook.md` and `docs/windows-backend-build.md` — packaging/handoff verification context.

Follow TDD where practical. Do not make Jira/status/doc claims without running verification. Do not print secrets. Keep UI minimal; Joseph prefers compact, less-is-more UI.

Critical architecture rules:

- Renderer never touches network/database directly. Calls go renderer -> preload bridge -> main IPC -> FastAPI.
- Backend owns data and persistence.
- Every mutation needs audit logging where backend state changes.
- Finalise/email/delivery failures must not destroy transcript/summary/participants.
- Manual recording and upload must remain available for in-room/ad-hoc use.
- Auto-recording is organizer-only and should not trigger for non-organizers.
- Audio-first; no video affordances.

## Verification baseline just run from WSL repo

These passed before this handoff was created:

```bash
npm run typecheck
PYTHONPATH=backend backend/.venv/bin/python -m unittest discover -s backend/tests -t backend -v  # 51 tests OK
npm run verify:graph  # Graph fixture verification passed
```

Run at least these after changes. For release/packaging work also run build/package checks appropriate to the touched area.

## Jira IN-64 board snapshot pulled live

Parent:

- IN-64 — Slice 1: Auto-Triggered Recording, Pyannote Speaker ID, and UI Modernisation — Story — In Progress — assignee David Ahlhaus — priority High.

Board items and Jira status at pull time:

Done / passed in Jira:

- IN-73 — evaluate removal of meeting list and calendar strip — Done
- IN-70 — internal vs client meeting_type rules — Done
- IN-97 — Get org account for pyannote — Done
- IN-96 — communicate with DV/Gabby on scope/handover/comms — Done
- IN-82 — confirm PyannoteAI API availability/credentials/costs — Done
- IN-65 — Graph meeting detection spike — Done
- IN-66 — auto-start/auto-stop wiring — Done
- IN-67 — host-only recording gate — Done
- IN-68 — MS Graph meeting detection — Done
- IN-98 — Graph polling restrictions — Done
- IN-95 — define Outlook emailing — Done
- IN-93 — implement Outlook emailing — Done
- IN-72 — remove per-calendar Record button — Done
- IN-74 — remove full meeting detail page — Done
- IN-75 — remove action items view/page — Done
- IN-77 — auto-recording status UI — Done
- IN-76 — admin voiceprint upload/registration utility — Done
- IN-92 — F1-wide comms owner alignment — Done
- IN-78 — attendee-first voiceprint candidate selection — Done
- IN-89 — Intune requirements — Done
- IN-84 — non-organizer auto-recording suppressed — Passed
- IN-85 — manual recording still works — Passed
- IN-87 — unregistered speaker stored anonymous — Passed
- IN-106 — agent summary instructions — Done
- IN-115 — register voiceprints — Passed
- IN-121 — reinstate manual audio upload — Done
- IN-122 — branch/merge rich HTML formatted email — Done

Open in Jira or needing reconciliation:

- IN-71 — Windows startup + tray persistence — In Progress in Jira, but repo evidence says implemented. Likely needs final installed-app/cold-boot verification or Jira closure.
- IN-91 — private SharePoint directory — In Progress in Jira, repo has local + Graph provider and Innovations SharePoint folder config path. Dedicated org directory still dependent on IT; continue current directory unless IT provides new one.
- IN-69 — Pyannote transcription + voiceprint identification — Planned in Jira, but repo has extensive implementation/tests/live smoke. Check if Jira needs status reconciliation; do not redo from scratch.
- IN-79 — voiceprint candidate expansion — In Progress in Jira, but repo has implementation and recent fix. Jira comment: expansion list temporarily includes all test users because manual recordings lack a central attendee DB.
- IN-80 — false-positive suppression — In Progress in Jira, but repo has threshold/overlap/unknown-reason tests and recent cluster-propagation fixes.
- IN-81 — packaging/signing/installer update — In Progress. Implementation mostly done; remaining real blockers are Windows build/installer validation, signing cert/CI release/Intune wrap, and manual rollout checklist.
- IN-83 — auto-recording starts for meeting organizer — In Progress. Needs live retest/evidence with current installer.
- IN-86 — known speaker identified by voiceprint with high confidence — Failed. This is the highest-risk technical item. David reported Pyannote job `39336e51-c56c-44ef-8588-1abad8d6939c`: BB + DA meeting did not separate speakers well; huge chunks; David voiceprint confidence very low. Hypothesis from Jira: multiple voiceprints for one speaker and none/weak for another may reduce confidence, voiceprint scripts too short/not unique enough. Repo has recent fixes: `f501307`, `bb257a1` for cluster propagation / diarization speaker-count hint. Needs fresh clean evidence, not assumption.
- IN-88 — UI cleanup test — Failed in Jira due wording ambiguity: David marked green for no per-meeting Record button/detail/action pages/audio setup, red because tray menu and ad-hoc recording still contain manual Start/Stop. But IN-64 explicitly says manual controls are kept. Likely requires clarification/comment or retest, not removal of manual controls.
- IN-94 — transcript email recipients — In Progress. IN-119 bug created for organizer missing; repo has fix and tests. Needs live retest.
- IN-114 — bespoke OpenAI API key — Planned; external/IT task.
- IN-116 — wording fixes — Draft. Repo has commit capitalising Meeting Notetaker; Jira comment says fix confirmed, left open for possible additional wording.
- IN-117 — extend recording function — In Progress. Repo has implementation and later toast/tray extension commits. Needs live validation and maybe polish.
- IN-118 — strip silence cost saving — Draft. Explicitly future/non-Slice-1; do not implement unless Joseph directs.
- IN-119 — bug: scheduled meeting as organizer, email not sent to organizer — In Progress. Repo fix exists; live retest needed.
- IN-120 — add pause/resume + stop buttons into tray menu — Draft in Jira, repo has implementation commit `40f474a`. Needs test/Jira reconciliation.
- IN-123 — Invite team button has no action — Draft. Repo hides it as requested.
- IN-124 — manual recording extension test — In Progress. Needs validation of extension behavior.
- IN-125 — IT handover and user guide — Draft. Non-code/high-priority handoff docs.
- IN-126 — additional UI cleanup — Planned. Repo has home cleanup commit; verify current UI against Jira list.
- IN-127 — transcript name defaults Unknown 1 to Speaker 1 — Draft. Repo has implementation commit; verify no remaining user-facing Unknown labels except docs/mock comments.

## Recent repo commits relevant to open Jira items

```text
bb257a1 fix: guard cluster propagation + add diarization speaker-count hint (IN-86)
c8b643d seed: add Jose Tabbada voiceprint (3 clips, precision-2)
f501307 fix: propagate cluster identity to reduce phantom Unknowns (IN-86)
513587e feat: extend scheduled recording from tray menu and toast button (IN-124)
8a2f359 feat: label unidentified speakers 'Speaker N' not 'Unknown N' (IN-127)
f9e3fc2 feat: home screen cleanup — hide invite, rename, remove link, upload button, show button (IN-123/IN-126)
98519e1 fix: watchdog for stalled pipelines + longer renderer processing window
40f474a feat: pause/resume/stop recording controls in tray menu (IN-120)
cfa408a Merge feature/rich-email-summary: rich HTML meeting-notes email (IN-122)
eecdbb6 feat: reinstate manual audio upload on home screen (IN-121)
7143d0d fix: manual recording title in tray tooltip (IN-77 'auto or manual')
1f85714 fix: tray tooltip shows meeting title + skipped-not-host state (IN-77/IN-83/IN-84)
b2ed75c feat: extend auto-recording before scheduled end (IN-117)
32da4d0 fix: capitalise 'Notetaker' in title bar and login (IN-116)
2ff76eb feat: toast notification when auto-recording starts (IN-83)
39e5ccf fix: email transcript to the organiser too (IN-94/IN-119)
```

## Likely remaining work today

Prioritize evidence and reconciliation over rewriting features already present.

### P0 — Reconcile live Jira failures with current code

1. IN-86 known speaker voiceprint failure
   - Root-cause before changing code.
   - Inspect recent speaker matching changes in `backend/app/services/speaker_matching.py`, speech/pyannote provider code, and tests.
   - Verify whether recent commits `f501307` and `bb257a1` address David's failed scenario.
   - If possible, run a clean live/local pipeline smoke using current voiceprints and a representative BB/DA/Joseph recording. Do not fabricate provider results.
   - Look for these failure modes:
     - pyannote diarization collapsed two speakers into one huge segment/chunk;
     - candidate list missing one speaker;
     - too-short voiceprint samples produce low confidence;
     - cluster propagation over- or under-applies identity;
     - configured `MN_VOICEPRINT_EXPANSION_EMPLOYEE_IDS` too broad/narrow for manual recordings.
   - Acceptance target: known enrolled speaker is named only when evidence is strong; weak/non-attendee detections remain `Speaker N` with diagnostic reason. If Pyannote quality remains genuinely insufficient, document the blocker honestly and propose voiceprint/enrollment/provider setting remediation.

2. IN-94 / IN-119 email organizer bug
   - Code already includes organizer + recorder in `_email_recipients()` in `backend/app/routers/meetings.py` and tests in `backend/tests/test_email_recipients.py`.
   - Verify scheduled meeting recipients include attendee(s), organizer, and recorder without duplicates.
   - Verify ad-hoc/manual sends only to recorder once.
   - If live Graph token is unavailable, at least run unit tests and document that live Graph retest is pending.

3. IN-88 UI cleanup failed status
   - Do not remove manual Start/Stop controls: IN-64 says they must be kept.
   - Current failure likely reflects Jira tester wording, not product bug. Prepare a Jira comment/evidence: per-meeting calendar Record button is gone; detail/action pages gone; manual tray/ad-hoc controls remain by acceptance criteria.

### P1 — Finish/verify recent UX and recording-control items

4. IN-117 / IN-124 extension
   - Current implementation:
     - preload `extendRecording()` and `onRecordingEndExtended()`.
     - main `recording-ipc.ts` schedules auto-stop, 5-min reminder, extend 10 min, and toast button via Windows toast XML.
     - tray has `Extend 10 min` when `hasExtendableRecording()`.
   - Verify UI countdown/end time updates on extension from renderer, tray, and toast action if possible.
   - Ensure extension applies only to active auto-recordings with scheduled end; manual recordings should not have auto-stop extension UI unless explicitly intended.

5. IN-120 pause/resume/stop in tray
   - Current tray menu exposes Pause/Resume, Extend when applicable, Stop recording while state is recording.
   - Verify renderer receives `recording:tray-control` and behaves correctly in `App.tsx`/`RecordingScreen.tsx`.

6. IN-123 / IN-126 additional UI cleanup
   - Current `HomeScreen.tsx` has:
     - `Meeting Title` section title;
     - `Start Recording` button;
     - `Upload recording` secondary button;
     - no visible invite-team button;
     - top recording banner with `Show` button.
   - Verify the meeting link field is removed from visible UI or unused; avoid leaving dead required props.
   - Decide whether People tab should remain. Jira says can be hidden if not required, but rollout runbook still uses People to verify seeded voiceprints and Jose T enrollment. Prefer keeping People unless Joseph explicitly says hide.

7. IN-127 Speaker labels
   - Backend stub and speaker matching now use `Speaker N`. Verify final transcript/email/user-facing text no longer says `Unknown 1` except historical docs/mock comments/tests where harmless.

### P2 — Packaging/handoff

8. IN-81 package/release verification
   - WSL cannot cross-compile the Windows backend; use Windows native Python/build if packaging.
   - Read `docs/windows-backend-build.md` exactly.
   - `npm run package:win` must be run from Windows repo with `backend.env` staging and bundled ffmpeg present.
   - For local builds, `electron-builder.yml` disables executable signing metadata if Windows symlink privilege is unavailable. CI/signing cert still needed for signed installer.
   - After build, run `scripts/smoke-backend-bundle.ps1` and verify packaged app starts backend, no sample data, tray idle, recording pipeline works.

9. IN-125 IT handover/user guide
   - Use `docs/rollout-runbook.md` as base.
   - Needs concise test-team/user guide: install, sign in, audio setup pitfalls, voiceprint enrollment, manual recording/upload, auto-record expectations, email/sharepoint output, troubleshooting.
   - Do not include secrets.

## Key files to inspect/edit

Frontend/Electron:

- `src/main/recording-ipc.ts` — auto-start/stop, extend, reminder toast, tray recording-control bridge.
- `src/main/tray.ts` — tray tooltip/menu controls.
- `src/main/index.ts` — IPC handlers, app launch/activation hooks.
- `src/preload/index.ts` — renderer bridge API.
- `src/renderer/src/App.tsx` — recording lifecycle, auto-start/stop, email/sharepoint after pipeline ready.
- `src/renderer/src/screens/HomeScreen.tsx` — UI cleanup/manual recording/upload.
- `src/renderer/src/screens/RecordingScreen.tsx` — pause/resume/stop/extend UI/countdown.
- `src/renderer/src/components/AppShell.tsx` or nav components — People tab/nav visibility if touched.

Backend:

- `backend/app/routers/meetings.py` — email/sharepoint endpoints, recipient resolution.
- `backend/app/services/speaker_matching.py` — attendee-first, expansion, false-positive suppression, cluster propagation.
- `backend/app/services/speech.py`, pyannote-related provider files — transcription/diarization/identify behavior.
- `backend/app/config.py` — provider/env settings.
- `backend/tests/test_email_recipients.py`, `backend/tests/test_speaker_identity_matching.py`, related tests.

Docs:

- `docs/jira-progress.md` — update only after verified changes.
- `docs/rollout-runbook.md` — handoff/testing guide.
- `docs/windows-backend-build.md` — Windows packaging.

## Commands

From repo root in WSL:

```bash
npm run typecheck
npm run build
npm run verify:graph
PYTHONPATH=backend backend/.venv/bin/python -m unittest discover -s backend/tests -t backend -v
backend/.venv/bin/python -m compileall backend/app backend/tests
git diff --check
```

Windows packaging (run on Windows filesystem/native tools, not WSL PyInstaller):

```cmd
cd /d C:\path\to\meeting-notetaker-2
npm run package:win
powershell -ExecutionPolicy Bypass -File scripts\smoke-backend-bundle.ps1
```

## Suggested Claude Code prompt

Use this as the actual task prompt if launching Claude Code/Fable 5.5:

```text
You are Fable 5.5 running in Claude Code on the Meeting Notetaker repo.

Goal: finish IN-64 remaining Slice 1 work today by reconciling Jira open/failing items against the current repo, fixing only real gaps, and producing verified evidence. Do not redo features that are already implemented.

Read, in order: AGENTS.md, CLAUDE.md, docs/jira-progress.md, docs/agent-handoffs/2026-07-10-in64-fable55-brief.md.

Priority:
1. Root-cause and resolve or honestly document IN-86 known-speaker voiceprint failure. Recent commits may already address cluster/diarization behavior; verify with tests and any available live/provider evidence. Never fake provider results.
2. Verify IN-94/IN-119 email recipient behavior: scheduled recordings include organizer + attendees + recorder without duplicates; ad-hoc/manual emails recorder once.
3. Verify/reconcile IN-88: manual Start/Stop controls must remain; failure likely needs evidence/comment, not removal.
4. Verify/polish IN-117/IN-124 extension and IN-120 tray pause/resume/stop behavior.
5. Verify/polish IN-123/IN-126 UI cleanup and IN-127 Speaker N labels.
6. If code is changed, update tests first where practical, implement minimally, then run full verification.
7. Update docs/jira-progress.md only for verified facts. Provide a final handoff with exact commands run and pass/fail output summary.

Hard constraints:
- Renderer never calls network/database directly.
- Do not expose secrets.
- Keep manual recording/upload available.
- Keep People tab unless Joseph explicitly decides to hide it, because rollout uses it for voiceprint verification/enrollment.
- Do not mark Jira items complete unless live/app evidence supports it.

Verification minimum after changes:
npm run typecheck
npm run build
npm run verify:graph
PYTHONPATH=backend backend/.venv/bin/python -m unittest discover -s backend/tests -t backend -v
backend/.venv/bin/python -m compileall backend/app backend/tests
git diff --check
```
